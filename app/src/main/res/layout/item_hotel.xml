<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    android:layout_marginStart="12dp"
    android:layout_marginEnd="12dp"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/background_item_hotel">
    <com.makeramen.roundedimageview.RoundedImageView
        android:layout_width="120dp"
        android:layout_height="80dp"
        android:id="@+id/avatar"
        tools:src="@drawable/hh"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="10dp"
        app:riv_border_width="0dp"
        app:riv_corner_radius="6dp"
        />
    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/name"
        tools:text="Hotel Saigon"
        android:textStyle="bold"
        android:textSize="15sp"
        app:layout_constraintStart_toEndOf="@id/avatar"
        app:layout_constraintTop_toTopOf="@id/avatar"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="16dp"
        android:layout_marginStart="6dp"
        />
    <RatingBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/rating_list_hotel"
        android:isIndicator="true"
        tools:rating="4"
        android:theme="@style/RatingBar"
        android:numStars="5"
        style="@android:style/Widget.DeviceDefault.Light.RatingBar.Small"
        app:layout_constraintStart_toStartOf="@id/name"
        app:layout_constraintTop_toBottomOf="@id/name"
        app:layout_constraintBottom_toTopOf="@id/address"
        app:layout_constraintVertical_chainStyle="packed"
        />
    <TextView
        android:id="@+id/address"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="#888"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@id/name"
        app:layout_constraintEnd_toStartOf="@id/price"
        tools:text="Quận 10, Hồ Chí Minh"
        app:layout_constraintTop_toBottomOf="@id/rating_list_hotel"
        app:layout_constraintBottom_toTopOf="@id/detail"
        app:layout_constraintVertical_chainStyle="packed"/>
    <TextView
        android:id="@+id/detail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="#888"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@id/name"
        app:layout_constraintEnd_toStartOf="@id/from_price"
        tools:text="090345235"
        app:layout_constraintTop_toBottomOf="@id/address"
        app:layout_constraintBottom_toBottomOf="@id/avatar"/>
    <TextView
        android:id="@+id/from_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="#666"
        app:layout_constraintEnd_toStartOf="@id/price"
        android:text="from "
        android:layout_marginEnd="4dp"
        app:layout_constraintTop_toTopOf="@id/price"
        app:layout_constraintBottom_toBottomOf="@id/price"/>
    <TextView
        android:id="@+id/price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@id/name"
        tools:text="33.000 đ"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/FlatBlue"
        app:layout_constraintTop_toTopOf="@id/detail"
        app:layout_constraintBottom_toBottomOf="@id/detail"/>
</android.support.constraint.ConstraintLayout>